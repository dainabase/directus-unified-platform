name: Quick NPM Publish (Disabled)

# This workflow is currently disabled and will be removed in next cleanup
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled'
        required: false
        type: string

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "❌ This workflow is disabled and scheduled for removal"
          echo "Please use npm-publish-ui.yml instead"
          exit 1
