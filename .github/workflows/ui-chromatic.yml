name: UI Chromatic
on:
  push:
    branches: [ main, develop ]
    paths:
      - 'packages/ui/src/**'
      - '.storybook/**'
      - 'packages/ui/.storybook/**'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'packages/ui/src/**'
      - '.storybook/**'
      - 'packages/ui/.storybook/**'
  workflow_dispatch:  # Allow manual trigger

jobs:
  chromatic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Required for Chromatic
      
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
      
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      
      - name: Build Storybook
        run: pnpm --filter @dainabase/ui build-storybook
      
      - name: Run Chromatic
        id: chromatic
        uses: chromaui/action@v1
        with:
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN || 'chpt_3606195941442a3' }}
          storybookBuildDir: packages/ui/storybook-static
          autoAcceptChanges: main  # Auto-accept changes on main branch only
          onlyChanged: true
      
      - name: Chromatic Results
        if: always()
        run: |
          echo "🎨 Chromatic Results:"
          echo "Build URL: ${{ steps.chromatic.outputs.buildUrl }}"
          echo "Storybook URL: ${{ steps.chromatic.outputs.storybookUrl }}"
          echo "Changes: ${{ steps.chromatic.outputs.changeCount }}"
          
      - name: Comment PR with Chromatic URLs
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const chromatic = {
              buildUrl: '${{ steps.chromatic.outputs.buildUrl }}',
              storybookUrl: '${{ steps.chromatic.outputs.storybookUrl }}',
              changes: '${{ steps.chromatic.outputs.changeCount }}'
            };
            
            const body = `## 🎨 Chromatic Visual Testing
            
            | Status | Value |
            |--------|-------|
            | **Build** | [View Build](${chromatic.buildUrl}) |
            | **Storybook** | [View Storybook](${chromatic.storybookUrl}) |
            | **Changes Detected** | ${chromatic.changes || '0'} |
            
            ${chromatic.changes > 0 ? '⚠️ Visual changes detected - review required' : '✅ No visual changes detected'}`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });
