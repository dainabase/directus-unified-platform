version: '3.8'

services:
  docker-mcp:
    image: ghcr.io/docker/mcp-server-docker:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - DEBUG=true
    ports:
      - "3000:3000"
    restart: unless-stopped